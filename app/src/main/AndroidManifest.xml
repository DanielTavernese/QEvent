<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dan_t.mine">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="QEvent (Current Events)"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SearchActivity"
            android:label="QEvent (Search Events)">
        </activity>

        <activity
            android:name=".EventOptionActivity"
            android:label="QHacks">
        </activity>

        <activity
            android:name=".AddEventActivity"
            android:label="QHacks Add Event Option">
        </activity>


        <activity
            android:name=".AddNewEventActivity"
            android:label="QEvent Add Event Option">
        </activity>

        <activity
            android:name=".CardviewFood"
            android:label="QEvent List of Foods">
        </activity>

        <activity
            android:name=".SearchNewEventActivity"
            android:label="QEvent Options Added">
    </activity>

    </application>



</manifest>